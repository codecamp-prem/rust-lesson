/// Color availiable for use
enum Color{
    Red,
    Blue,
}

/// Mail struct 
struct Mail{
    /// destination address
    address: String,
}

 /// Adds two numbers
 fn add(a: i32, b: i32) -> i32 {
    a + b
 }

 fn main(){}
